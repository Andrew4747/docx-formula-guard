# Example report summary

Running the checker on `sample_input.tex` identifies:

- a multi-line LaTeX environment;
- an explicit spacing command;
- a duplicate equation number `(3-1)`;
- a possible numbering gap at `(3-2)`.

These findings are precautionary. They do not state that the mathematics is
incorrect or that conversion must fail.
